FROM python:3.11-slim

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir -e . fastapi uvicorn

ENV PYTHONUNBUFFERED=1

EXPOSE 8000

CMD ["uvicorn", "api.perfect_agent_api:app", "--host", "0.0.0.0", "--port", "8000"]
